import {connect} from 'react-redux';

import ListingNavigation from './ListedBookingNavigation.jsx';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(mapStateToProps, mapDispatchToProps)(ListingNavigation);
